/*
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.hanzo.cloud.model;

import java.util.Objects;
import ai.hanzo.cloud.model.Market;
import ai.hanzo.cloud.model.Reach;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.hanzo.cloud.JSON;

/**
 * Roster
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.14.0")
public class Roster {
  public static final String SERIALIZED_NAME_CHAINS = "chains";
  @SerializedName(SERIALIZED_NAME_CHAINS)
  @javax.annotation.Nullable
  private List<Market> chains = new ArrayList<>();

  public static final String SERIALIZED_NAME_REACH = "reach";
  @SerializedName(SERIALIZED_NAME_REACH)
  @javax.annotation.Nullable
  private Reach reach;

  public Roster() {
  }

  public Roster chains(@javax.annotation.Nullable List<Market> chains) {
    this.chains = chains;
    return this;
  }

  public Roster addChainsItem(Market chainsItem) {
    if (this.chains == null) {
      this.chains = new ArrayList<>();
    }
    this.chains.add(chainsItem);
    return this;
  }

  /**
   * Chains is &#x60;[]&#x60; where the registry answered and named none, and &#x60;null&#x60; where it did not answer — never absent, because a missing key and an empty list read alike and only one of them means \&quot;there are none\&quot;. Each row carries its OWN reach for its figures, so a chain whose indexer is down is one row saying so.
   * @return chains
   */
  @javax.annotation.Nullable
  public List<Market> getChains() {
    return chains;
  }

  public void setChains(@javax.annotation.Nullable List<Market> chains) {
    this.chains = chains;
  }


  public Roster reach(@javax.annotation.Nullable Reach reach) {
    this.reach = reach;
    return this;
  }

  /**
   * Reach is how far the read of the REGISTRY got. It governs the list: a registry that did not answer yields no rows, and the reason it did not is here rather than in an empty array a caller would read as \&quot;no chains exist\&quot;.
   * @return reach
   */
  @javax.annotation.Nullable
  public Reach getReach() {
    return reach;
  }

  public void setReach(@javax.annotation.Nullable Reach reach) {
    this.reach = reach;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    Roster roster = (Roster) o;
    return Objects.equals(this.chains, roster.chains) &&
        Objects.equals(this.reach, roster.reach);
  }

  @Override
  public int hashCode() {
    return Objects.hash(chains, reach);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class Roster {\n");
    sb.append("    chains: ").append(toIndentedString(chains)).append("\n");
    sb.append("    reach: ").append(toIndentedString(reach)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("chains", "reach"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(0);
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to Roster
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!Roster.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in Roster is not found in the empty JSON string", Roster.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!Roster.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `Roster` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("chains") != null && !jsonObj.get("chains").isJsonNull()) {
        JsonArray jsonArraychains = jsonObj.getAsJsonArray("chains");
        if (jsonArraychains != null) {
          // ensure the json data is an array
          if (!jsonObj.get("chains").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `chains` to be an array in the JSON string but got `%s`", jsonObj.get("chains").toString()));
          }

          // validate the optional field `chains` (array)
          for (int i = 0; i < jsonArraychains.size(); i++) {
            Market.validateJsonElement(jsonArraychains.get(i));
          };
        }
      }
      // validate the optional field `reach`
      if (jsonObj.get("reach") != null && !jsonObj.get("reach").isJsonNull()) {
        Reach.validateJsonElement(jsonObj.get("reach"));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!Roster.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'Roster' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<Roster> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(Roster.class));

       return (TypeAdapter<T>) new TypeAdapter<Roster>() {
           @Override
           public void write(JsonWriter out, Roster value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public Roster read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of Roster given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of Roster
   * @throws IOException if the JSON string is invalid with respect to Roster
   */
  public static Roster fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, Roster.class);
  }

  /**
   * Convert an instance of Roster to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

