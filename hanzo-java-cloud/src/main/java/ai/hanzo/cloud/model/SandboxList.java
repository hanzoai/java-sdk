/*
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.hanzo.cloud.model;

import java.util.Objects;
import ai.hanzo.cloud.model.Sandbox;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.hanzo.cloud.JSON;

/**
 * SandboxList
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.14.0")
public class SandboxList {
  public static final String SERIALIZED_NAME_SANDBOXES = "sandboxes";
  @SerializedName(SERIALIZED_NAME_SANDBOXES)
  @javax.annotation.Nullable
  private List<Sandbox> sandboxes = new ArrayList<>();

  public SandboxList() {
  }

  public SandboxList sandboxes(@javax.annotation.Nullable List<Sandbox> sandboxes) {
    this.sandboxes = sandboxes;
    return this;
  }

  public SandboxList addSandboxesItem(Sandbox sandboxesItem) {
    if (this.sandboxes == null) {
      this.sandboxes = new ArrayList<>();
    }
    this.sandboxes.add(sandboxesItem);
    return this;
  }

  /**
   * Sandboxes are the caller org&#39;s sandboxes matching the filter. Never null.
   * @return sandboxes
   */
  @javax.annotation.Nullable
  public List<Sandbox> getSandboxes() {
    return sandboxes;
  }

  public void setSandboxes(@javax.annotation.Nullable List<Sandbox> sandboxes) {
    this.sandboxes = sandboxes;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    SandboxList sandboxList = (SandboxList) o;
    return Objects.equals(this.sandboxes, sandboxList.sandboxes);
  }

  @Override
  public int hashCode() {
    return Objects.hash(sandboxes);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class SandboxList {\n");
    sb.append("    sandboxes: ").append(toIndentedString(sandboxes)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("sandboxes"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(0);
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to SandboxList
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!SandboxList.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in SandboxList is not found in the empty JSON string", SandboxList.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!SandboxList.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `SandboxList` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("sandboxes") != null && !jsonObj.get("sandboxes").isJsonNull()) {
        JsonArray jsonArraysandboxes = jsonObj.getAsJsonArray("sandboxes");
        if (jsonArraysandboxes != null) {
          // ensure the json data is an array
          if (!jsonObj.get("sandboxes").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `sandboxes` to be an array in the JSON string but got `%s`", jsonObj.get("sandboxes").toString()));
          }

          // validate the optional field `sandboxes` (array)
          for (int i = 0; i < jsonArraysandboxes.size(); i++) {
            Sandbox.validateJsonElement(jsonArraysandboxes.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!SandboxList.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'SandboxList' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<SandboxList> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(SandboxList.class));

       return (TypeAdapter<T>) new TypeAdapter<SandboxList>() {
           @Override
           public void write(JsonWriter out, SandboxList value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public SandboxList read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of SandboxList given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of SandboxList
   * @throws IOException if the JSON string is invalid with respect to SandboxList
   */
  public static SandboxList fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, SandboxList.class);
  }

  /**
   * Convert an instance of SandboxList to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

