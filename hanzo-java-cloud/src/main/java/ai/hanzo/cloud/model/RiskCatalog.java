/*
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.hanzo.cloud.model;

import java.util.Objects;
import ai.hanzo.cloud.model.RiskBand;
import ai.hanzo.cloud.model.RiskModelFeature;
import ai.hanzo.cloud.model.RiskOrgFeature;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.hanzo.cloud.JSON;

/**
 * RiskCatalog
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.14.0")
public class RiskCatalog {
  public static final String SERIALIZED_NAME_GAP = "gap";
  @SerializedName(SERIALIZED_NAME_GAP)
  @javax.annotation.Nullable
  private String gap;

  public static final String SERIALIZED_NAME_MODEL = "model";
  @SerializedName(SERIALIZED_NAME_MODEL)
  @javax.annotation.Nullable
  private List<RiskModelFeature> model = new ArrayList<>();

  public static final String SERIALIZED_NAME_NETWORK = "network";
  @SerializedName(SERIALIZED_NAME_NETWORK)
  @javax.annotation.Nullable
  private List<RiskBand> network = new ArrayList<>();

  public static final String SERIALIZED_NAME_SURFACE = "surface";
  @SerializedName(SERIALIZED_NAME_SURFACE)
  @javax.annotation.Nullable
  private List<RiskOrgFeature> surface = new ArrayList<>();

  public static final String SERIALIZED_NAME_TENANT = "tenant";
  @SerializedName(SERIALIZED_NAME_TENANT)
  @javax.annotation.Nullable
  private String tenant;

  public RiskCatalog() {
  }

  public RiskCatalog gap(@javax.annotation.Nullable String gap) {
    this.gap = gap;
    return this;
  }

  /**
   * Gap says why a lens could not be measured, when that is the case. Each reason names its own lens, because \&quot;the surface is unreadable\&quot; and \&quot;the network baseline is unreadable\&quot; are different facts.
   * @return gap
   */
  @javax.annotation.Nullable
  public String getGap() {
    return gap;
  }

  public void setGap(@javax.annotation.Nullable String gap) {
    this.gap = gap;
  }


  public RiskCatalog model(@javax.annotation.Nullable List<RiskModelFeature> model) {
    this.model = model;
    return this;
  }

  public RiskCatalog addModelItem(RiskModelFeature modelItem) {
    if (this.model == null) {
      this.model = new ArrayList<>();
    }
    this.model.add(modelItem);
    return this;
  }

  /**
   * Model is the governed inventory: one entry per dimension of the model space, each carrying the typology it serves and the published standard that asks for it. It is the same for every organisation, because it is the model&#39;s shape.
   * @return model
   */
  @javax.annotation.Nullable
  public List<RiskModelFeature> getModel() {
    return model;
  }

  public void setModel(@javax.annotation.Nullable List<RiskModelFeature> model) {
    this.model = model;
  }


  public RiskCatalog network(@javax.annotation.Nullable List<RiskBand> network) {
    this.network = network;
    return this;
  }

  public RiskCatalog addNetworkItem(RiskBand networkItem) {
    if (this.network == null) {
      this.network = new ArrayList<>();
    }
    this.network.add(networkItem);
    return this;
  }

  /**
   * Network is the published cross-organisation baseline over the same window, so the surface above has something to be read AGAINST. It is the same for every caller and it names nobody.  It carries no tenant and cannot be made to: the table it reads has no org column, every figure is a quantile over at least kAnonOrgs organisations weighted one vote each, and a band that does not meet that floor is dropped on the way out.
   * @return network
   */
  @javax.annotation.Nullable
  public List<RiskBand> getNetwork() {
    return network;
  }

  public void setNetwork(@javax.annotation.Nullable List<RiskBand> network) {
    this.network = network;
  }


  public RiskCatalog surface(@javax.annotation.Nullable List<RiskOrgFeature> surface) {
    this.surface = surface;
    return this;
  }

  public RiskCatalog addSurfaceItem(RiskOrgFeature surfaceItem) {
    if (this.surface == null) {
      this.surface = new ArrayList<>();
    }
    this.surface.add(surfaceItem);
    return this;
  }

  /**
   * Surface is what this organisation&#39;s own event surface carries, per dimension, measured over the window. A dimension present in no bucket is blind here — the model reads its neutral value and a reviewer has to be able to see that.
   * @return surface
   */
  @javax.annotation.Nullable
  public List<RiskOrgFeature> getSurface() {
    return surface;
  }

  public void setSurface(@javax.annotation.Nullable List<RiskOrgFeature> surface) {
    this.surface = surface;
  }


  public RiskCatalog tenant(@javax.annotation.Nullable String tenant) {
    this.tenant = tenant;
    return this;
  }

  /**
   * Tenant is whose surface was measured.
   * @return tenant
   */
  @javax.annotation.Nullable
  public String getTenant() {
    return tenant;
  }

  public void setTenant(@javax.annotation.Nullable String tenant) {
    this.tenant = tenant;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    RiskCatalog riskCatalog = (RiskCatalog) o;
    return Objects.equals(this.gap, riskCatalog.gap) &&
        Objects.equals(this.model, riskCatalog.model) &&
        Objects.equals(this.network, riskCatalog.network) &&
        Objects.equals(this.surface, riskCatalog.surface) &&
        Objects.equals(this.tenant, riskCatalog.tenant);
  }

  @Override
  public int hashCode() {
    return Objects.hash(gap, model, network, surface, tenant);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class RiskCatalog {\n");
    sb.append("    gap: ").append(toIndentedString(gap)).append("\n");
    sb.append("    model: ").append(toIndentedString(model)).append("\n");
    sb.append("    network: ").append(toIndentedString(network)).append("\n");
    sb.append("    surface: ").append(toIndentedString(surface)).append("\n");
    sb.append("    tenant: ").append(toIndentedString(tenant)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("gap", "model", "network", "surface", "tenant"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(0);
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to RiskCatalog
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!RiskCatalog.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in RiskCatalog is not found in the empty JSON string", RiskCatalog.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!RiskCatalog.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `RiskCatalog` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if ((jsonObj.get("gap") != null && !jsonObj.get("gap").isJsonNull()) && !jsonObj.get("gap").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `gap` to be a primitive type in the JSON string but got `%s`", jsonObj.get("gap").toString()));
      }
      if (jsonObj.get("model") != null && !jsonObj.get("model").isJsonNull()) {
        JsonArray jsonArraymodel = jsonObj.getAsJsonArray("model");
        if (jsonArraymodel != null) {
          // ensure the json data is an array
          if (!jsonObj.get("model").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `model` to be an array in the JSON string but got `%s`", jsonObj.get("model").toString()));
          }

          // validate the optional field `model` (array)
          for (int i = 0; i < jsonArraymodel.size(); i++) {
            RiskModelFeature.validateJsonElement(jsonArraymodel.get(i));
          };
        }
      }
      if (jsonObj.get("network") != null && !jsonObj.get("network").isJsonNull()) {
        JsonArray jsonArraynetwork = jsonObj.getAsJsonArray("network");
        if (jsonArraynetwork != null) {
          // ensure the json data is an array
          if (!jsonObj.get("network").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `network` to be an array in the JSON string but got `%s`", jsonObj.get("network").toString()));
          }

          // validate the optional field `network` (array)
          for (int i = 0; i < jsonArraynetwork.size(); i++) {
            RiskBand.validateJsonElement(jsonArraynetwork.get(i));
          };
        }
      }
      if (jsonObj.get("surface") != null && !jsonObj.get("surface").isJsonNull()) {
        JsonArray jsonArraysurface = jsonObj.getAsJsonArray("surface");
        if (jsonArraysurface != null) {
          // ensure the json data is an array
          if (!jsonObj.get("surface").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `surface` to be an array in the JSON string but got `%s`", jsonObj.get("surface").toString()));
          }

          // validate the optional field `surface` (array)
          for (int i = 0; i < jsonArraysurface.size(); i++) {
            RiskOrgFeature.validateJsonElement(jsonArraysurface.get(i));
          };
        }
      }
      if ((jsonObj.get("tenant") != null && !jsonObj.get("tenant").isJsonNull()) && !jsonObj.get("tenant").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `tenant` to be a primitive type in the JSON string but got `%s`", jsonObj.get("tenant").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!RiskCatalog.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'RiskCatalog' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<RiskCatalog> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(RiskCatalog.class));

       return (TypeAdapter<T>) new TypeAdapter<RiskCatalog>() {
           @Override
           public void write(JsonWriter out, RiskCatalog value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public RiskCatalog read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of RiskCatalog given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of RiskCatalog
   * @throws IOException if the JSON string is invalid with respect to RiskCatalog
   */
  public static RiskCatalog fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, RiskCatalog.class);
  }

  /**
   * Convert an instance of RiskCatalog to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

