/*
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.hanzo.cloud.model;

import java.util.Objects;
import java.math.BigDecimal;
import java.util.List;
import java.util.Map;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import ai.hanzo.cloud.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.14.0")
public class VectorVectorInput extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(VectorVectorInput.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!VectorVectorInput.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'VectorVectorInput' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);

            final Type typeInstanceListBigDecimal = new TypeToken<List<BigDecimal>>(){}.getType();
            final TypeAdapter<List<BigDecimal>> adapterListBigDecimal = (TypeAdapter<List<BigDecimal>>) gson.getDelegateAdapter(this, TypeToken.get(typeInstanceListBigDecimal));
            final Type typeInstanceMapStringListBigDecimal = new TypeToken<Map<String, List<BigDecimal>>>(){}.getType();
            final TypeAdapter<Map<String, List<BigDecimal>>> adapterMapStringListBigDecimal = (TypeAdapter<Map<String, List<BigDecimal>>>) gson.getDelegateAdapter(this, TypeToken.get(typeInstanceMapStringListBigDecimal));

            return (TypeAdapter<T>) new TypeAdapter<VectorVectorInput>() {
                @Override
                public void write(JsonWriter out, VectorVectorInput value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `List<BigDecimal>`
                    if (value.getActualInstance() instanceof List<?>) {
                        List<?> list = (List<?>) value.getActualInstance();
                        if (list.get(0) instanceof BigDecimal) {
                            JsonArray array = adapterListBigDecimal.toJsonTree((List<BigDecimal>)value.getActualInstance()).getAsJsonArray();
                            elementAdapter.write(out, array);
                            return;
                        }
                    }
                    // check if the actual instance is of the type `Map<String, List<BigDecimal>>`
                    if (value.getActualInstance() instanceof Map<?, ?>) {
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: List<BigDecimal>, Map<String, List<BigDecimal>>");
                }

                @Override
                public VectorVectorInput read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize List<BigDecimal>
                    try {
                        // validate the JSON object to see if any exception is thrown
                        if (!jsonElement.isJsonArray()) {
                            throw new IllegalArgumentException(String.format("Expected json element to be a array type in the JSON string but got `%s`", jsonElement.toString()));
                        }

                        JsonArray array = jsonElement.getAsJsonArray();
                        // validate array items
                        for(JsonElement element : array) {
                            if (!jsonElement.getAsJsonPrimitive().isNumber()) {
                                throw new IllegalArgumentException(String.format("Expected json element to be of type Number in the JSON string but got `%s`", jsonElement.toString()));
                            }
                        }
                        actualAdapter = adapterListBigDecimal;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'List<BigDecimal>'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for List<BigDecimal> failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'List<BigDecimal>'", e);
                    }
                    // deserialize Map<String, List<BigDecimal>>
                    try {
                        // validate the JSON object to see if any exception is thrown
                        if (!jsonElement.isJsonObject()) {
                            throw new IllegalArgumentException(String.format("Expected json element to be a object type in the JSON string but got `%s`", jsonElement.toString()));
                        }

                        actualAdapter = adapterMapStringListBigDecimal;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'Map<String, List<BigDecimal>>'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for Map<String, List<BigDecimal>> failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'Map<String, List<BigDecimal>>'", e);
                    }

                    if (match == 1) {
                        VectorVectorInput ret = new VectorVectorInput();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for VectorVectorInput: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public VectorVectorInput() {
        super("oneOf", Boolean.FALSE);
    }

    public VectorVectorInput(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("List<BigDecimal>", List.class);
        schemas.put("Map<String, List<BigDecimal>>", Map.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return VectorVectorInput.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * List<BigDecimal>, Map<String, List<BigDecimal>>
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof List<?>) {
            List<?> list = (List<?>) instance;
            if (list.get(0) instanceof BigDecimal) {
                super.setActualInstance(instance);
                return;
            }
        }

        if (instance instanceof Map<?, ?>) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be List<BigDecimal>, Map<String, List<BigDecimal>>");
    }

    /**
     * Get the actual instance, which can be the following:
     * List<BigDecimal>, Map<String, List<BigDecimal>>
     *
     * @return The actual instance (List<BigDecimal>, Map<String, List<BigDecimal>>)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `List<BigDecimal>`. If the actual instance is not `List<BigDecimal>`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `List<BigDecimal>`
     * @throws ClassCastException if the instance is not `List<BigDecimal>`
     */
    public List<BigDecimal> getListBigDecimal() throws ClassCastException {
        return (List<BigDecimal>)super.getActualInstance();
    }

    /**
     * Get the actual instance of `Map<String, List<BigDecimal>>`. If the actual instance is not `Map<String, List<BigDecimal>>`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Map<String, List<BigDecimal>>`
     * @throws ClassCastException if the instance is not `Map<String, List<BigDecimal>>`
     */
    public Map<String, List<BigDecimal>> getMapStringListBigDecimal() throws ClassCastException {
        return (Map<String, List<BigDecimal>>)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to VectorVectorInput
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with List<BigDecimal>
        try {
            if (!jsonElement.isJsonArray()) {
                throw new IllegalArgumentException(String.format("Expected json element to be a array type in the JSON string but got `%s`", jsonElement.toString()));
            }
            JsonArray array = jsonElement.getAsJsonArray();
            // validate array items
            for(JsonElement element : array) {
                if (!jsonElement.getAsJsonPrimitive().isNumber()) {
                    throw new IllegalArgumentException(String.format("Expected json element to be of type Number in the JSON string but got `%s`", jsonElement.toString()));
                }
            }
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for List<BigDecimal> failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with Map<String, List<BigDecimal>>
        try {
            if (!jsonElement.isJsonObject()) {
                throw new IllegalArgumentException(String.format("Expected json element to be a object type in the JSON string but got `%s`", jsonElement.toString()));
            }

            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for Map<String, List<BigDecimal>> failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format("The JSON string is invalid for VectorVectorInput with oneOf schemas: List<BigDecimal>, Map<String, List<BigDecimal>>. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of VectorVectorInput given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of VectorVectorInput
     * @throws IOException if the JSON string is invalid with respect to VectorVectorInput
     */
    public static VectorVectorInput fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, VectorVectorInput.class);
    }

    /**
     * Convert an instance of VectorVectorInput to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

