/*
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.hanzo.cloud.model;

import java.util.Objects;
import ai.hanzo.cloud.model.Formation;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.hanzo.cloud.JSON;

/**
 * FormationView
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.14.0")
public class FormationView {
  public static final String SERIALIZED_NAME_FORMATION = "formation";
  @SerializedName(SERIALIZED_NAME_FORMATION)
  @javax.annotation.Nullable
  private Formation formation;

  public static final String SERIALIZED_NAME_NEXT_STAGES = "nextStages";
  @SerializedName(SERIALIZED_NAME_NEXT_STAGES)
  @javax.annotation.Nullable
  private List<String> nextStages = new ArrayList<>();

  public FormationView() {
  }

  public FormationView formation(@javax.annotation.Nullable Formation formation) {
    this.formation = formation;
    return this;
  }

  /**
   * Formation is the org&#39;s one incorporation record.
   * @return formation
   */
  @javax.annotation.Nullable
  public Formation getFormation() {
    return formation;
  }

  public void setFormation(@javax.annotation.Nullable Formation formation) {
    this.formation = formation;
  }


  public FormationView nextStages(@javax.annotation.Nullable List<String> nextStages) {
    this.nextStages = nextStages;
    return this;
  }

  public FormationView addNextStagesItem(String nextStagesItem) {
    if (this.nextStages == null) {
      this.nextStages = new ArrayList<>();
    }
    this.nextStages.add(nextStagesItem);
    return this;
  }

  /**
   * NextStages are the stages reachable from the formation&#39;s current stage, whether or not their guards are satisfied yet.
   * @return nextStages
   */
  @javax.annotation.Nullable
  public List<String> getNextStages() {
    return nextStages;
  }

  public void setNextStages(@javax.annotation.Nullable List<String> nextStages) {
    this.nextStages = nextStages;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    FormationView formationView = (FormationView) o;
    return Objects.equals(this.formation, formationView.formation) &&
        Objects.equals(this.nextStages, formationView.nextStages);
  }

  @Override
  public int hashCode() {
    return Objects.hash(formation, nextStages);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class FormationView {\n");
    sb.append("    formation: ").append(toIndentedString(formation)).append("\n");
    sb.append("    nextStages: ").append(toIndentedString(nextStages)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("formation", "nextStages"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(0);
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to FormationView
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!FormationView.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in FormationView is not found in the empty JSON string", FormationView.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!FormationView.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `FormationView` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // validate the optional field `formation`
      if (jsonObj.get("formation") != null && !jsonObj.get("formation").isJsonNull()) {
        Formation.validateJsonElement(jsonObj.get("formation"));
      }
      // ensure the optional json data is an array if present
      if (jsonObj.get("nextStages") != null && !jsonObj.get("nextStages").isJsonNull() && !jsonObj.get("nextStages").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `nextStages` to be an array in the JSON string but got `%s`", jsonObj.get("nextStages").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!FormationView.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'FormationView' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<FormationView> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(FormationView.class));

       return (TypeAdapter<T>) new TypeAdapter<FormationView>() {
           @Override
           public void write(JsonWriter out, FormationView value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public FormationView read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of FormationView given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of FormationView
   * @throws IOException if the JSON string is invalid with respect to FormationView
   */
  public static FormationView fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, FormationView.class);
  }

  /**
   * Convert an instance of FormationView to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

