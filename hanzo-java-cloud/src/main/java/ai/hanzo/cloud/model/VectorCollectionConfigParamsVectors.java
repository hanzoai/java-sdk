/*
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.hanzo.cloud.model;

import java.util.Objects;
import ai.hanzo.cloud.model.VectorVectorParams;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;
import java.util.Map;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import ai.hanzo.cloud.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.14.0")
public class VectorCollectionConfigParamsVectors extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(VectorCollectionConfigParamsVectors.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!VectorCollectionConfigParamsVectors.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'VectorCollectionConfigParamsVectors' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<VectorVectorParams> adapterVectorVectorParams = gson.getDelegateAdapter(this, TypeToken.get(VectorVectorParams.class));
            final Type typeInstanceMapStringVectorVectorParams = new TypeToken<Map<String, VectorVectorParams>>(){}.getType();
            final TypeAdapter<Map<String, VectorVectorParams>> adapterMapStringVectorVectorParams = (TypeAdapter<Map<String, VectorVectorParams>>) gson.getDelegateAdapter(this, TypeToken.get(typeInstanceMapStringVectorVectorParams));

            return (TypeAdapter<T>) new TypeAdapter<VectorCollectionConfigParamsVectors>() {
                @Override
                public void write(JsonWriter out, VectorCollectionConfigParamsVectors value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `VectorVectorParams`
                    if (value.getActualInstance() instanceof VectorVectorParams) {
                        JsonElement element = adapterVectorVectorParams.toJsonTree((VectorVectorParams)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `Map<String, VectorVectorParams>`
                    if (value.getActualInstance() instanceof Map<?, ?>) {
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: Map<String, VectorVectorParams>, VectorVectorParams");
                }

                @Override
                public VectorCollectionConfigParamsVectors read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize VectorVectorParams
                    try {
                        // validate the JSON object to see if any exception is thrown
                        VectorVectorParams.validateJsonElement(jsonElement);
                        actualAdapter = adapterVectorVectorParams;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'VectorVectorParams'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for VectorVectorParams failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'VectorVectorParams'", e);
                    }
                    // deserialize Map<String, VectorVectorParams>
                    try {
                        // validate the JSON object to see if any exception is thrown
                        if (!jsonElement.isJsonObject()) {
                            throw new IllegalArgumentException(String.format("Expected json element to be a object type in the JSON string but got `%s`", jsonElement.toString()));
                        }

                        actualAdapter = adapterMapStringVectorVectorParams;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'Map<String, VectorVectorParams>'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for Map<String, VectorVectorParams> failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'Map<String, VectorVectorParams>'", e);
                    }

                    if (match == 1) {
                        VectorCollectionConfigParamsVectors ret = new VectorCollectionConfigParamsVectors();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for VectorCollectionConfigParamsVectors: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public VectorCollectionConfigParamsVectors() {
        super("oneOf", Boolean.FALSE);
    }

    public VectorCollectionConfigParamsVectors(Object o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("VectorVectorParams", VectorVectorParams.class);
        schemas.put("Map<String, VectorVectorParams>", Map.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return VectorCollectionConfigParamsVectors.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * Map<String, VectorVectorParams>, VectorVectorParams
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof VectorVectorParams) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof Map<?, ?>) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be Map<String, VectorVectorParams>, VectorVectorParams");
    }

    /**
     * Get the actual instance, which can be the following:
     * Map<String, VectorVectorParams>, VectorVectorParams
     *
     * @return The actual instance (Map<String, VectorVectorParams>, VectorVectorParams)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `VectorVectorParams`. If the actual instance is not `VectorVectorParams`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `VectorVectorParams`
     * @throws ClassCastException if the instance is not `VectorVectorParams`
     */
    public VectorVectorParams getVectorVectorParams() throws ClassCastException {
        return (VectorVectorParams)super.getActualInstance();
    }

    /**
     * Get the actual instance of `Map<String, VectorVectorParams>`. If the actual instance is not `Map<String, VectorVectorParams>`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `Map<String, VectorVectorParams>`
     * @throws ClassCastException if the instance is not `Map<String, VectorVectorParams>`
     */
    public Map<String, VectorVectorParams> getMapStringVectorVectorParams() throws ClassCastException {
        return (Map<String, VectorVectorParams>)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to VectorCollectionConfigParamsVectors
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with VectorVectorParams
        try {
            VectorVectorParams.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for VectorVectorParams failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with Map<String, VectorVectorParams>
        try {
            if (!jsonElement.isJsonObject()) {
                throw new IllegalArgumentException(String.format("Expected json element to be a object type in the JSON string but got `%s`", jsonElement.toString()));
            }

            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for Map<String, VectorVectorParams> failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            throw new IOException(String.format("The JSON string is invalid for VectorCollectionConfigParamsVectors with oneOf schemas: Map<String, VectorVectorParams>, VectorVectorParams. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of VectorCollectionConfigParamsVectors given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of VectorCollectionConfigParamsVectors
     * @throws IOException if the JSON string is invalid with respect to VectorCollectionConfigParamsVectors
     */
    public static VectorCollectionConfigParamsVectors fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, VectorCollectionConfigParamsVectors.class);
    }

    /**
     * Convert an instance of VectorCollectionConfigParamsVectors to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

