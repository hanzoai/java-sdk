/*
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.hanzo.cloud.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.hanzo.cloud.JSON;

/**
 * StateGraph
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.14.0")
public class StateGraph {
  public static final String SERIALIZED_NAME_INITIAL = "initial";
  @SerializedName(SERIALIZED_NAME_INITIAL)
  @javax.annotation.Nullable
  private String initial;

  public static final String SERIALIZED_NAME_LIVE = "live";
  @SerializedName(SERIALIZED_NAME_LIVE)
  @javax.annotation.Nullable
  private String live;

  public static final String SERIALIZED_NAME_STATES = "states";
  @SerializedName(SERIALIZED_NAME_STATES)
  @javax.annotation.Nullable
  private List<String> states = new ArrayList<>();

  public static final String SERIALIZED_NAME_TRANSITIONS = "transitions";
  @SerializedName(SERIALIZED_NAME_TRANSITIONS)
  @javax.annotation.Nullable
  private Map<String, List<String>> transitions = new HashMap<>();

  public StateGraph() {
  }

  public StateGraph initial(@javax.annotation.Nullable String initial) {
    this.initial = initial;
    return this;
  }

  /**
   * Initial is the state a fresh document starts in — \&quot;draft\&quot;. A stored document with no status at all is read as this too.
   * @return initial
   */
  @javax.annotation.Nullable
  public String getInitial() {
    return initial;
  }

  public void setInitial(@javax.annotation.Nullable String initial) {
    this.initial = initial;
  }


  public StateGraph live(@javax.annotation.Nullable String live) {
    this.live = live;
    return this;
  }

  /**
   * Live is the ONE state that is publicly readable — \&quot;published\&quot;. The site pulls only documents in it, so reaching Live IS site-publish; every other state is invisible to a reader.
   * @return live
   */
  @javax.annotation.Nullable
  public String getLive() {
    return live;
  }

  public void setLive(@javax.annotation.Nullable String live) {
    this.live = live;
  }


  public StateGraph states(@javax.annotation.Nullable List<String> states) {
    this.states = states;
    return this;
  }

  public StateGraph addStatesItem(String statesItem) {
    if (this.states == null) {
      this.states = new ArrayList<>();
    }
    this.states.add(statesItem);
    return this;
  }

  /**
   * States is every lifecycle state in canonical order: draft, in_review, approved, queued, published, archived. The console lays its board columns out in exactly this order, so the order is part of the answer.
   * @return states
   */
  @javax.annotation.Nullable
  public List<String> getStates() {
    return states;
  }

  public void setStates(@javax.annotation.Nullable List<String> states) {
    this.states = states;
  }


  public StateGraph transitions(@javax.annotation.Nullable Map<String, List<String>> transitions) {
    this.transitions = transitions;
    return this;
  }

  public StateGraph putTransitionsItem(String key, List<String> transitionsItem) {
    if (this.transitions == null) {
      this.transitions = new HashMap<>();
    }
    this.transitions.put(key, transitionsItem);
    return this;
  }

  /**
   * Transitions maps each state to the states it may move to. A target absent from a state&#39;s list is REFUSED, at the endpoint and again at the storage boundary — this is the whole rule, not a hint for the UI. A state never lists itself; a move that changes nothing is always legal.
   * @return transitions
   */
  @javax.annotation.Nullable
  public Map<String, List<String>> getTransitions() {
    return transitions;
  }

  public void setTransitions(@javax.annotation.Nullable Map<String, List<String>> transitions) {
    this.transitions = transitions;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    StateGraph stateGraph = (StateGraph) o;
    return Objects.equals(this.initial, stateGraph.initial) &&
        Objects.equals(this.live, stateGraph.live) &&
        Objects.equals(this.states, stateGraph.states) &&
        Objects.equals(this.transitions, stateGraph.transitions);
  }

  @Override
  public int hashCode() {
    return Objects.hash(initial, live, states, transitions);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class StateGraph {\n");
    sb.append("    initial: ").append(toIndentedString(initial)).append("\n");
    sb.append("    live: ").append(toIndentedString(live)).append("\n");
    sb.append("    states: ").append(toIndentedString(states)).append("\n");
    sb.append("    transitions: ").append(toIndentedString(transitions)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("initial", "live", "states", "transitions"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(0);
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to StateGraph
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!StateGraph.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in StateGraph is not found in the empty JSON string", StateGraph.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!StateGraph.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `StateGraph` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if ((jsonObj.get("initial") != null && !jsonObj.get("initial").isJsonNull()) && !jsonObj.get("initial").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `initial` to be a primitive type in the JSON string but got `%s`", jsonObj.get("initial").toString()));
      }
      if ((jsonObj.get("live") != null && !jsonObj.get("live").isJsonNull()) && !jsonObj.get("live").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `live` to be a primitive type in the JSON string but got `%s`", jsonObj.get("live").toString()));
      }
      // ensure the optional json data is an array if present
      if (jsonObj.get("states") != null && !jsonObj.get("states").isJsonNull() && !jsonObj.get("states").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `states` to be an array in the JSON string but got `%s`", jsonObj.get("states").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!StateGraph.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'StateGraph' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<StateGraph> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(StateGraph.class));

       return (TypeAdapter<T>) new TypeAdapter<StateGraph>() {
           @Override
           public void write(JsonWriter out, StateGraph value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public StateGraph read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of StateGraph given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of StateGraph
   * @throws IOException if the JSON string is invalid with respect to StateGraph
   */
  public static StateGraph fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, StateGraph.class);
  }

  /**
   * Convert an instance of StateGraph to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

