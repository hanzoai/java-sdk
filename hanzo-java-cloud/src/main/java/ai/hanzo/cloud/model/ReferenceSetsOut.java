/*
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay doors, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.hanzo.cloud.model;

import java.util.Objects;
import ai.hanzo.cloud.model.ReferenceSet;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.hanzo.cloud.JSON;

/**
 * ReferenceSetsOut
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.14.0")
public class ReferenceSetsOut {
  public static final String SERIALIZED_NAME_REFUSED = "refused";
  @SerializedName(SERIALIZED_NAME_REFUSED)
  @javax.annotation.Nullable
  private List<String> refused = new ArrayList<>();

  public static final String SERIALIZED_NAME_SETS = "sets";
  @SerializedName(SERIALIZED_NAME_SETS)
  @javax.annotation.Nullable
  private List<ReferenceSet> sets = new ArrayList<>();

  public static final String SERIALIZED_NAME_STALE = "stale";
  @SerializedName(SERIALIZED_NAME_STALE)
  @javax.annotation.Nullable
  private List<String> stale = new ArrayList<>();

  public ReferenceSetsOut() {
  }

  public ReferenceSetsOut refused(@javax.annotation.Nullable List<String> refused) {
    this.refused = refused;
    return this;
  }

  public ReferenceSetsOut addRefusedItem(String refusedItem) {
    if (this.refused == null) {
      this.refused = new ArrayList<>();
    }
    this.refused.add(refusedItem);
    return this;
  }

  /**
   * Refused names the sets that cannot be consulted at all. A key checked against one of these is UNKNOWN, not clean.
   * @return refused
   */
  @javax.annotation.Nullable
  public List<String> getRefused() {
    return refused;
  }

  public void setRefused(@javax.annotation.Nullable List<String> refused) {
    this.refused = refused;
  }


  public ReferenceSetsOut sets(@javax.annotation.Nullable List<ReferenceSet> sets) {
    this.sets = sets;
    return this;
  }

  public ReferenceSetsOut addSetsItem(ReferenceSet setsItem) {
    if (this.sets == null) {
      this.sets = new ArrayList<>();
    }
    this.sets.add(setsItem);
    return this;
  }

  /**
   * Sets is the whole catalog, in a stable order.
   * @return sets
   */
  @javax.annotation.Nullable
  public List<ReferenceSet> getSets() {
    return sets;
  }

  public void setSets(@javax.annotation.Nullable List<ReferenceSet> sets) {
    this.sets = sets;
  }


  public ReferenceSetsOut stale(@javax.annotation.Nullable List<String> stale) {
    this.stale = stale;
    return this;
  }

  public ReferenceSetsOut addStaleItem(String staleItem) {
    if (this.stale == null) {
      this.stale = new ArrayList<>();
    }
    this.stale.add(staleItem);
    return this;
  }

  /**
   * Stale names the sets past their freshness bound — the list to alarm on.
   * @return stale
   */
  @javax.annotation.Nullable
  public List<String> getStale() {
    return stale;
  }

  public void setStale(@javax.annotation.Nullable List<String> stale) {
    this.stale = stale;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    ReferenceSetsOut referenceSetsOut = (ReferenceSetsOut) o;
    return Objects.equals(this.refused, referenceSetsOut.refused) &&
        Objects.equals(this.sets, referenceSetsOut.sets) &&
        Objects.equals(this.stale, referenceSetsOut.stale);
  }

  @Override
  public int hashCode() {
    return Objects.hash(refused, sets, stale);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class ReferenceSetsOut {\n");
    sb.append("    refused: ").append(toIndentedString(refused)).append("\n");
    sb.append("    sets: ").append(toIndentedString(sets)).append("\n");
    sb.append("    stale: ").append(toIndentedString(stale)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("refused", "sets", "stale"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(0);
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to ReferenceSetsOut
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!ReferenceSetsOut.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in ReferenceSetsOut is not found in the empty JSON string", ReferenceSetsOut.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!ReferenceSetsOut.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `ReferenceSetsOut` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // ensure the optional json data is an array if present
      if (jsonObj.get("refused") != null && !jsonObj.get("refused").isJsonNull() && !jsonObj.get("refused").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `refused` to be an array in the JSON string but got `%s`", jsonObj.get("refused").toString()));
      }
      if (jsonObj.get("sets") != null && !jsonObj.get("sets").isJsonNull()) {
        JsonArray jsonArraysets = jsonObj.getAsJsonArray("sets");
        if (jsonArraysets != null) {
          // ensure the json data is an array
          if (!jsonObj.get("sets").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `sets` to be an array in the JSON string but got `%s`", jsonObj.get("sets").toString()));
          }

          // validate the optional field `sets` (array)
          for (int i = 0; i < jsonArraysets.size(); i++) {
            ReferenceSet.validateJsonElement(jsonArraysets.get(i));
          };
        }
      }
      // ensure the optional json data is an array if present
      if (jsonObj.get("stale") != null && !jsonObj.get("stale").isJsonNull() && !jsonObj.get("stale").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `stale` to be an array in the JSON string but got `%s`", jsonObj.get("stale").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!ReferenceSetsOut.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'ReferenceSetsOut' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<ReferenceSetsOut> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(ReferenceSetsOut.class));

       return (TypeAdapter<T>) new TypeAdapter<ReferenceSetsOut>() {
           @Override
           public void write(JsonWriter out, ReferenceSetsOut value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public ReferenceSetsOut read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of ReferenceSetsOut given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of ReferenceSetsOut
   * @throws IOException if the JSON string is invalid with respect to ReferenceSetsOut
   */
  public static ReferenceSetsOut fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, ReferenceSetsOut.class);
  }

  /**
   * Convert an instance of ReferenceSetsOut to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

