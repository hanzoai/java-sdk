/*
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.hanzo.cloud.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.hanzo.cloud.JSON;

/**
 * RiskSourceCoverage
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.14.0")
public class RiskSourceCoverage {
  public static final String SERIALIZED_NAME_FACTS = "facts";
  @SerializedName(SERIALIZED_NAME_FACTS)
  @javax.annotation.Nullable
  private Integer facts;

  public static final String SERIALIZED_NAME_SOURCE = "source";
  @SerializedName(SERIALIZED_NAME_SOURCE)
  @javax.annotation.Nullable
  private String source;

  public static final String SERIALIZED_NAME_WON = "won";
  @SerializedName(SERIALIZED_NAME_WON)
  @javax.annotation.Nullable
  private Integer won;

  public RiskSourceCoverage() {
  }

  public RiskSourceCoverage facts(@javax.annotation.Nullable Integer facts) {
    this.facts = facts;
    return this;
  }

  /**
   * Facts is how many assertions this source filed; Won is how many judged events it was the assertion in force for. A source with many facts and few wins is one that is being outranked, which is worth knowing before concluding it is wired correctly.
   * @return facts
   */
  @javax.annotation.Nullable
  public Integer getFacts() {
    return facts;
  }

  public void setFacts(@javax.annotation.Nullable Integer facts) {
    this.facts = facts;
  }


  public RiskSourceCoverage source(@javax.annotation.Nullable String source) {
    this.source = source;
    return this;
  }

  /**
   * Source is the asserter these two counts are for — chargeoff, dispute, case, refund, review or sample. There is one entry per source that either filed in the window or won in it, in precedence order, strongest first. A source no longer in the vocabulary still has rows and is reported after the known ones rather than dropped out of a total that is supposed to add up.
   * @return source
   */
  @javax.annotation.Nullable
  public String getSource() {
    return source;
  }

  public void setSource(@javax.annotation.Nullable String source) {
    this.source = source;
  }


  public RiskSourceCoverage won(@javax.annotation.Nullable Integer won) {
    this.won = won;
    return this;
  }

  /**
   * Won is how many JUDGED events this source&#39;s assertion was the one IN FORCE for, at that event&#39;s own as-of — it beat every other visible claim under the precedence rule. Summed over the sources it is Judged. Read against Facts it is the ratio that matters: many filed and few won is a source being outranked, not a source that is broken, and one source winning nearly everything is a plane that looks labelled because one noisy filer dominates it.
   * @return won
   */
  @javax.annotation.Nullable
  public Integer getWon() {
    return won;
  }

  public void setWon(@javax.annotation.Nullable Integer won) {
    this.won = won;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    RiskSourceCoverage riskSourceCoverage = (RiskSourceCoverage) o;
    return Objects.equals(this.facts, riskSourceCoverage.facts) &&
        Objects.equals(this.source, riskSourceCoverage.source) &&
        Objects.equals(this.won, riskSourceCoverage.won);
  }

  @Override
  public int hashCode() {
    return Objects.hash(facts, source, won);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class RiskSourceCoverage {\n");
    sb.append("    facts: ").append(toIndentedString(facts)).append("\n");
    sb.append("    source: ").append(toIndentedString(source)).append("\n");
    sb.append("    won: ").append(toIndentedString(won)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("facts", "source", "won"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(0);
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to RiskSourceCoverage
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!RiskSourceCoverage.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in RiskSourceCoverage is not found in the empty JSON string", RiskSourceCoverage.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!RiskSourceCoverage.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `RiskSourceCoverage` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if ((jsonObj.get("source") != null && !jsonObj.get("source").isJsonNull()) && !jsonObj.get("source").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `source` to be a primitive type in the JSON string but got `%s`", jsonObj.get("source").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!RiskSourceCoverage.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'RiskSourceCoverage' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<RiskSourceCoverage> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(RiskSourceCoverage.class));

       return (TypeAdapter<T>) new TypeAdapter<RiskSourceCoverage>() {
           @Override
           public void write(JsonWriter out, RiskSourceCoverage value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public RiskSourceCoverage read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of RiskSourceCoverage given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of RiskSourceCoverage
   * @throws IOException if the JSON string is invalid with respect to RiskSourceCoverage
   */
  public static RiskSourceCoverage fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, RiskSourceCoverage.class);
  }

  /**
   * Convert an instance of RiskSourceCoverage to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

