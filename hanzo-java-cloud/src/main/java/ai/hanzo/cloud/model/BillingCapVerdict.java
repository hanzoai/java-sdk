/*
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.hanzo.cloud.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.hanzo.cloud.JSON;

/**
 * The per-request spend-cap verdict for a (project, service) scope, evaluated over the org&#39;s spend-alert rows (most-restrictive-wins). Fails CLOSED: if a hard-enforceable row&#39;s spend cannot be computed the verdict denies. 
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.14.0")
public class BillingCapVerdict {
  public static final String SERIALIZED_NAME_ALLOW = "allow";
  @SerializedName(SERIALIZED_NAME_ALLOW)
  @javax.annotation.Nonnull
  private Boolean allow;

  public static final String SERIALIZED_NAME_REASON = "reason";
  @SerializedName(SERIALIZED_NAME_REASON)
  @javax.annotation.Nonnull
  private String reason;

  public static final String SERIALIZED_NAME_CAP_CENTS = "capCents";
  @SerializedName(SERIALIZED_NAME_CAP_CENTS)
  @javax.annotation.Nullable
  private Long capCents;

  public static final String SERIALIZED_NAME_SPENT_CENTS = "spentCents";
  @SerializedName(SERIALIZED_NAME_SPENT_CENTS)
  @javax.annotation.Nullable
  private Long spentCents;

  public static final String SERIALIZED_NAME_WARN_PCT = "warnPct";
  @SerializedName(SERIALIZED_NAME_WARN_PCT)
  @javax.annotation.Nullable
  private Integer warnPct;

  public BillingCapVerdict() {
  }

  public BillingCapVerdict allow(@javax.annotation.Nonnull Boolean allow) {
    this.allow = allow;
    return this;
  }

  /**
   * Whether the request is permitted.
   * @return allow
   */
  @javax.annotation.Nonnull
  public Boolean getAllow() {
    return allow;
  }

  public void setAllow(@javax.annotation.Nonnull Boolean allow) {
    this.allow = allow;
  }


  public BillingCapVerdict reason(@javax.annotation.Nonnull String reason) {
    this.reason = reason;
    return this;
  }

  /**
   * The cap/funds verdict reason. \&quot;\&quot; &#x3D; allowed (funded and under every covering cap). &#x60;spend_cap&#x60; &#x3D; funded but a HARD-enforced per-scope cap would be exceeded (the tenant&#39;s OWN policy ceiling). &#x60;insufficient_balance&#x60; &#x3D; out of prepaid funds. &#x60;spend_cap&#x60; and &#x60;insufficient_balance&#x60; are DISTINCT — one is policy, one is funds. The &#x60;/spend-alerts/authorize&#x60; endpoint (a pure cap check) emits only \&quot;\&quot; or &#x60;spend_cap&#x60;; the cloud money-path composite (metering) adds &#x60;insufficient_balance&#x60;. 
   * @return reason
   */
  @javax.annotation.Nonnull
  public String getReason() {
    return reason;
  }

  public void setReason(@javax.annotation.Nonnull String reason) {
    this.reason = reason;
  }


  public BillingCapVerdict capCents(@javax.annotation.Nullable Long capCents) {
    this.capCents = capCents;
    return this;
  }

  /**
   * The tightest violated cap&#39;s ceiling in USD cents (0 when allowed).
   * @return capCents
   */
  @javax.annotation.Nullable
  public Long getCapCents() {
    return capCents;
  }

  public void setCapCents(@javax.annotation.Nullable Long capCents) {
    this.capCents = capCents;
  }


  public BillingCapVerdict spentCents(@javax.annotation.Nullable Long spentCents) {
    this.spentCents = spentCents;
    return this;
  }

  /**
   * Period spend for the scope in USD cents at decision time.
   * @return spentCents
   */
  @javax.annotation.Nullable
  public Long getSpentCents() {
    return spentCents;
  }

  public void setSpentCents(@javax.annotation.Nullable Long spentCents) {
    this.spentCents = spentCents;
  }


  public BillingCapVerdict warnPct(@javax.annotation.Nullable Integer warnPct) {
    this.warnPct = warnPct;
    return this;
  }

  /**
   * Utilization percent of the most-utilized covering cap when at/over its soft threshold (0 otherwise, and 0 on a deny).
   * @return warnPct
   */
  @javax.annotation.Nullable
  public Integer getWarnPct() {
    return warnPct;
  }

  public void setWarnPct(@javax.annotation.Nullable Integer warnPct) {
    this.warnPct = warnPct;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the BillingCapVerdict instance itself
   */
  public BillingCapVerdict putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    BillingCapVerdict billingCapVerdict = (BillingCapVerdict) o;
    return Objects.equals(this.allow, billingCapVerdict.allow) &&
        Objects.equals(this.reason, billingCapVerdict.reason) &&
        Objects.equals(this.capCents, billingCapVerdict.capCents) &&
        Objects.equals(this.spentCents, billingCapVerdict.spentCents) &&
        Objects.equals(this.warnPct, billingCapVerdict.warnPct)&&
        Objects.equals(this.additionalProperties, billingCapVerdict.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(allow, reason, capCents, spentCents, warnPct, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class BillingCapVerdict {\n");
    sb.append("    allow: ").append(toIndentedString(allow)).append("\n");
    sb.append("    reason: ").append(toIndentedString(reason)).append("\n");
    sb.append("    capCents: ").append(toIndentedString(capCents)).append("\n");
    sb.append("    spentCents: ").append(toIndentedString(spentCents)).append("\n");
    sb.append("    warnPct: ").append(toIndentedString(warnPct)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("allow", "reason", "capCents", "spentCents", "warnPct"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(Arrays.asList("allow", "reason"));
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to BillingCapVerdict
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!BillingCapVerdict.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in BillingCapVerdict is not found in the empty JSON string", BillingCapVerdict.openapiRequiredFields.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : BillingCapVerdict.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format("The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (!jsonObj.get("reason").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `reason` to be a primitive type in the JSON string but got `%s`", jsonObj.get("reason").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!BillingCapVerdict.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'BillingCapVerdict' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<BillingCapVerdict> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(BillingCapVerdict.class));

       return (TypeAdapter<T>) new TypeAdapter<BillingCapVerdict>() {
           @Override
           public void write(JsonWriter out, BillingCapVerdict value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public BillingCapVerdict read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             BillingCapVerdict instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of BillingCapVerdict given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of BillingCapVerdict
   * @throws IOException if the JSON string is invalid with respect to BillingCapVerdict
   */
  public static BillingCapVerdict fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, BillingCapVerdict.class);
  }

  /**
   * Convert an instance of BillingCapVerdict to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

