/*
 * Hanzo Cloud API
 * Composed from each subsystem's own projection of its router, in the fleet's mount order — every operation below is a route the subsystem that publishes it registered. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.hanzo.cloud.model;

import java.util.Objects;
import ai.hanzo.cloud.model.AdminReferralView;
import ai.hanzo.cloud.model.AdminSummary;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.hanzo.cloud.JSON;

/**
 * AdminBonusDirectory
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.14.0")
public class AdminBonusDirectory {
  public static final String SERIALIZED_NAME_REFERRALS = "referrals";
  @SerializedName(SERIALIZED_NAME_REFERRALS)
  @javax.annotation.Nullable
  private List<AdminReferralView> referrals = new ArrayList<>();

  public static final String SERIALIZED_NAME_SUMMARY = "summary";
  @SerializedName(SERIALIZED_NAME_SUMMARY)
  @javax.annotation.Nullable
  private AdminSummary summary;

  public AdminBonusDirectory() {
  }

  public AdminBonusDirectory referrals(@javax.annotation.Nullable List<AdminReferralView> referrals) {
    this.referrals = referrals;
    return this;
  }

  public AdminBonusDirectory addReferralsItem(AdminReferralView referralsItem) {
    if (this.referrals == null) {
      this.referrals = new ArrayList<>();
    }
    this.referrals.add(referralsItem);
    return this;
  }

  /**
   * Referrals is every referral in the directory, both orgs exposed.
   * @return referrals
   */
  @javax.annotation.Nullable
  public List<AdminReferralView> getReferrals() {
    return referrals;
  }

  public void setReferrals(@javax.annotation.Nullable List<AdminReferralView> referrals) {
    this.referrals = referrals;
  }


  public AdminBonusDirectory summary(@javax.annotation.Nullable AdminSummary summary) {
    this.summary = summary;
    return this;
  }

  /**
   * Summary is the fleet tally across those referrals.
   * @return summary
   */
  @javax.annotation.Nullable
  public AdminSummary getSummary() {
    return summary;
  }

  public void setSummary(@javax.annotation.Nullable AdminSummary summary) {
    this.summary = summary;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    AdminBonusDirectory adminBonusDirectory = (AdminBonusDirectory) o;
    return Objects.equals(this.referrals, adminBonusDirectory.referrals) &&
        Objects.equals(this.summary, adminBonusDirectory.summary);
  }

  @Override
  public int hashCode() {
    return Objects.hash(referrals, summary);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class AdminBonusDirectory {\n");
    sb.append("    referrals: ").append(toIndentedString(referrals)).append("\n");
    sb.append("    summary: ").append(toIndentedString(summary)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("referrals", "summary"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(0);
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to AdminBonusDirectory
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!AdminBonusDirectory.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in AdminBonusDirectory is not found in the empty JSON string", AdminBonusDirectory.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!AdminBonusDirectory.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `AdminBonusDirectory` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("referrals") != null && !jsonObj.get("referrals").isJsonNull()) {
        JsonArray jsonArrayreferrals = jsonObj.getAsJsonArray("referrals");
        if (jsonArrayreferrals != null) {
          // ensure the json data is an array
          if (!jsonObj.get("referrals").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `referrals` to be an array in the JSON string but got `%s`", jsonObj.get("referrals").toString()));
          }

          // validate the optional field `referrals` (array)
          for (int i = 0; i < jsonArrayreferrals.size(); i++) {
            AdminReferralView.validateJsonElement(jsonArrayreferrals.get(i));
          };
        }
      }
      // validate the optional field `summary`
      if (jsonObj.get("summary") != null && !jsonObj.get("summary").isJsonNull()) {
        AdminSummary.validateJsonElement(jsonObj.get("summary"));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!AdminBonusDirectory.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'AdminBonusDirectory' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<AdminBonusDirectory> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(AdminBonusDirectory.class));

       return (TypeAdapter<T>) new TypeAdapter<AdminBonusDirectory>() {
           @Override
           public void write(JsonWriter out, AdminBonusDirectory value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public AdminBonusDirectory read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of AdminBonusDirectory given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of AdminBonusDirectory
   * @throws IOException if the JSON string is invalid with respect to AdminBonusDirectory
   */
  public static AdminBonusDirectory fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, AdminBonusDirectory.class);
  }

  /**
   * Convert an instance of AdminBonusDirectory to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

