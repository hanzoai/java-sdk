/*
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.hanzo.cloud.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.hanzo.cloud.JSON;

/**
 * SubprocessorList
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.14.0")
public class SubprocessorList {
  public static final String SERIALIZED_NAME_SUBPROCESSORS = "subprocessors";
  @SerializedName(SERIALIZED_NAME_SUBPROCESSORS)
  @javax.annotation.Nullable
  private List<Object> subprocessors = new ArrayList<>();

  public SubprocessorList() {
  }

  public SubprocessorList subprocessors(@javax.annotation.Nullable List<Object> subprocessors) {
    this.subprocessors = subprocessors;
    return this;
  }

  public SubprocessorList addSubprocessorsItem(Object subprocessorsItem) {
    if (this.subprocessors == null) {
      this.subprocessors = new ArrayList<>();
    }
    this.subprocessors.add(subprocessorsItem);
    return this;
  }

  /**
   * Subprocessors is the list, each naming at least what it is and what it is for — a name alone says nothing.
   * @return subprocessors
   */
  @javax.annotation.Nullable
  public List<Object> getSubprocessors() {
    return subprocessors;
  }

  public void setSubprocessors(@javax.annotation.Nullable List<Object> subprocessors) {
    this.subprocessors = subprocessors;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    SubprocessorList subprocessorList = (SubprocessorList) o;
    return Objects.equals(this.subprocessors, subprocessorList.subprocessors);
  }

  @Override
  public int hashCode() {
    return Objects.hash(subprocessors);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class SubprocessorList {\n");
    sb.append("    subprocessors: ").append(toIndentedString(subprocessors)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("subprocessors"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(0);
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to SubprocessorList
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!SubprocessorList.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in SubprocessorList is not found in the empty JSON string", SubprocessorList.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!SubprocessorList.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `SubprocessorList` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // ensure the optional json data is an array if present
      if (jsonObj.get("subprocessors") != null && !jsonObj.get("subprocessors").isJsonNull() && !jsonObj.get("subprocessors").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `subprocessors` to be an array in the JSON string but got `%s`", jsonObj.get("subprocessors").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!SubprocessorList.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'SubprocessorList' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<SubprocessorList> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(SubprocessorList.class));

       return (TypeAdapter<T>) new TypeAdapter<SubprocessorList>() {
           @Override
           public void write(JsonWriter out, SubprocessorList value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public SubprocessorList read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of SubprocessorList given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of SubprocessorList
   * @throws IOException if the JSON string is invalid with respect to SubprocessorList
   */
  public static SubprocessorList fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, SubprocessorList.class);
  }

  /**
   * Convert an instance of SubprocessorList to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

