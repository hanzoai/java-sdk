/*
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.hanzo.cloud.model;

import java.util.Objects;
import ai.hanzo.cloud.model.VectorCondition;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.hanzo.cloud.JSON;

/**
 * VectorFilter
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.14.0")
public class VectorFilter {
  public static final String SERIALIZED_NAME_MUST = "must";
  @SerializedName(SERIALIZED_NAME_MUST)
  @javax.annotation.Nullable
  private List<VectorCondition> must = new ArrayList<>();

  public static final String SERIALIZED_NAME_SHOULD = "should";
  @SerializedName(SERIALIZED_NAME_SHOULD)
  @javax.annotation.Nullable
  private List<VectorCondition> should = new ArrayList<>();

  public static final String SERIALIZED_NAME_MUST_NOT = "must_not";
  @SerializedName(SERIALIZED_NAME_MUST_NOT)
  @javax.annotation.Nullable
  private List<VectorCondition> mustNot = new ArrayList<>();

  public VectorFilter() {
  }

  public VectorFilter must(@javax.annotation.Nullable List<VectorCondition> must) {
    this.must = must;
    return this;
  }

  public VectorFilter addMustItem(VectorCondition mustItem) {
    if (this.must == null) {
      this.must = new ArrayList<>();
    }
    this.must.add(mustItem);
    return this;
  }

  /**
   * Get must
   * @return must
   */
  @javax.annotation.Nullable
  public List<VectorCondition> getMust() {
    return must;
  }

  public void setMust(@javax.annotation.Nullable List<VectorCondition> must) {
    this.must = must;
  }


  public VectorFilter should(@javax.annotation.Nullable List<VectorCondition> should) {
    this.should = should;
    return this;
  }

  public VectorFilter addShouldItem(VectorCondition shouldItem) {
    if (this.should == null) {
      this.should = new ArrayList<>();
    }
    this.should.add(shouldItem);
    return this;
  }

  /**
   * Get should
   * @return should
   */
  @javax.annotation.Nullable
  public List<VectorCondition> getShould() {
    return should;
  }

  public void setShould(@javax.annotation.Nullable List<VectorCondition> should) {
    this.should = should;
  }


  public VectorFilter mustNot(@javax.annotation.Nullable List<VectorCondition> mustNot) {
    this.mustNot = mustNot;
    return this;
  }

  public VectorFilter addMustNotItem(VectorCondition mustNotItem) {
    if (this.mustNot == null) {
      this.mustNot = new ArrayList<>();
    }
    this.mustNot.add(mustNotItem);
    return this;
  }

  /**
   * Get mustNot
   * @return mustNot
   */
  @javax.annotation.Nullable
  public List<VectorCondition> getMustNot() {
    return mustNot;
  }

  public void setMustNot(@javax.annotation.Nullable List<VectorCondition> mustNot) {
    this.mustNot = mustNot;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    VectorFilter vectorFilter = (VectorFilter) o;
    return Objects.equals(this.must, vectorFilter.must) &&
        Objects.equals(this.should, vectorFilter.should) &&
        Objects.equals(this.mustNot, vectorFilter.mustNot);
  }

  @Override
  public int hashCode() {
    return Objects.hash(must, should, mustNot);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class VectorFilter {\n");
    sb.append("    must: ").append(toIndentedString(must)).append("\n");
    sb.append("    should: ").append(toIndentedString(should)).append("\n");
    sb.append("    mustNot: ").append(toIndentedString(mustNot)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("must", "should", "must_not"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(0);
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to VectorFilter
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!VectorFilter.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in VectorFilter is not found in the empty JSON string", VectorFilter.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!VectorFilter.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `VectorFilter` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("must") != null && !jsonObj.get("must").isJsonNull()) {
        JsonArray jsonArraymust = jsonObj.getAsJsonArray("must");
        if (jsonArraymust != null) {
          // ensure the json data is an array
          if (!jsonObj.get("must").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `must` to be an array in the JSON string but got `%s`", jsonObj.get("must").toString()));
          }

          // validate the optional field `must` (array)
          for (int i = 0; i < jsonArraymust.size(); i++) {
            VectorCondition.validateJsonElement(jsonArraymust.get(i));
          };
        }
      }
      if (jsonObj.get("should") != null && !jsonObj.get("should").isJsonNull()) {
        JsonArray jsonArrayshould = jsonObj.getAsJsonArray("should");
        if (jsonArrayshould != null) {
          // ensure the json data is an array
          if (!jsonObj.get("should").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `should` to be an array in the JSON string but got `%s`", jsonObj.get("should").toString()));
          }

          // validate the optional field `should` (array)
          for (int i = 0; i < jsonArrayshould.size(); i++) {
            VectorCondition.validateJsonElement(jsonArrayshould.get(i));
          };
        }
      }
      if (jsonObj.get("must_not") != null && !jsonObj.get("must_not").isJsonNull()) {
        JsonArray jsonArraymustNot = jsonObj.getAsJsonArray("must_not");
        if (jsonArraymustNot != null) {
          // ensure the json data is an array
          if (!jsonObj.get("must_not").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `must_not` to be an array in the JSON string but got `%s`", jsonObj.get("must_not").toString()));
          }

          // validate the optional field `must_not` (array)
          for (int i = 0; i < jsonArraymustNot.size(); i++) {
            VectorCondition.validateJsonElement(jsonArraymustNot.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!VectorFilter.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'VectorFilter' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<VectorFilter> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(VectorFilter.class));

       return (TypeAdapter<T>) new TypeAdapter<VectorFilter>() {
           @Override
           public void write(JsonWriter out, VectorFilter value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public VectorFilter read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of VectorFilter given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of VectorFilter
   * @throws IOException if the JSON string is invalid with respect to VectorFilter
   */
  public static VectorFilter fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, VectorFilter.class);
  }

  /**
   * Convert an instance of VectorFilter to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

