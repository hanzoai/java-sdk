/*
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.hanzo.cloud.model;

import java.util.Objects;
import ai.hanzo.cloud.model.ArgoNode;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.hanzo.cloud.JSON;

/**
 * ArgoTree
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.14.0")
public class ArgoTree {
  public static final String SERIALIZED_NAME_HOSTS = "hosts";
  @SerializedName(SERIALIZED_NAME_HOSTS)
  @javax.annotation.Nullable
  private List<Object> hosts = new ArrayList<>();

  public static final String SERIALIZED_NAME_NODES = "nodes";
  @SerializedName(SERIALIZED_NAME_NODES)
  @javax.annotation.Nullable
  private List<ArgoNode> nodes = new ArrayList<>();

  public static final String SERIALIZED_NAME_ORPHANED_NODES = "orphanedNodes";
  @SerializedName(SERIALIZED_NAME_ORPHANED_NODES)
  @javax.annotation.Nullable
  private List<ArgoNode> orphanedNodes = new ArrayList<>();

  public ArgoTree() {
  }

  public ArgoTree hosts(@javax.annotation.Nullable List<Object> hosts) {
    this.hosts = hosts;
    return this;
  }

  public ArgoTree addHostsItem(Object hostsItem) {
    if (this.hosts == null) {
      this.hosts = new ArrayList<>();
    }
    this.hosts.add(hostsItem);
    return this;
  }

  /**
   * Hosts is ArgoCD&#39;s per-node machine inventory. Always empty: this plane projects applications and serves no cluster-node view.
   * @return hosts
   */
  @javax.annotation.Nullable
  public List<Object> getHosts() {
    return hosts;
  }

  public void setHosts(@javax.annotation.Nullable List<Object> hosts) {
    this.hosts = hosts;
  }


  public ArgoTree nodes(@javax.annotation.Nullable List<ArgoNode> nodes) {
    this.nodes = nodes;
    return this;
  }

  public ArgoTree addNodesItem(ArgoNode nodesItem) {
    if (this.nodes == null) {
      this.nodes = new ArrayList<>();
    }
    this.nodes.add(nodesItem);
    return this;
  }

  /**
   * Nodes is the FLAT node list, root first: the App CR, then the objects the operator owns, then their ReplicaSets and Pods. The hierarchy is in ParentRefs, not in the ordering.
   * @return nodes
   */
  @javax.annotation.Nullable
  public List<ArgoNode> getNodes() {
    return nodes;
  }

  public void setNodes(@javax.annotation.Nullable List<ArgoNode> nodes) {
    this.nodes = nodes;
  }


  public ArgoTree orphanedNodes(@javax.annotation.Nullable List<ArgoNode> orphanedNodes) {
    this.orphanedNodes = orphanedNodes;
    return this;
  }

  public ArgoTree addOrphanedNodesItem(ArgoNode orphanedNodesItem) {
    if (this.orphanedNodes == null) {
      this.orphanedNodes = new ArrayList<>();
    }
    this.orphanedNodes.add(orphanedNodesItem);
    return this;
  }

  /**
   * OrphanedNodes are objects in the namespace belonging to no application. Always empty: this walk reaches an object only THROUGH ownership from the App CR, so it can never hold one that is orphaned.
   * @return orphanedNodes
   */
  @javax.annotation.Nullable
  public List<ArgoNode> getOrphanedNodes() {
    return orphanedNodes;
  }

  public void setOrphanedNodes(@javax.annotation.Nullable List<ArgoNode> orphanedNodes) {
    this.orphanedNodes = orphanedNodes;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    ArgoTree argoTree = (ArgoTree) o;
    return Objects.equals(this.hosts, argoTree.hosts) &&
        Objects.equals(this.nodes, argoTree.nodes) &&
        Objects.equals(this.orphanedNodes, argoTree.orphanedNodes);
  }

  @Override
  public int hashCode() {
    return Objects.hash(hosts, nodes, orphanedNodes);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class ArgoTree {\n");
    sb.append("    hosts: ").append(toIndentedString(hosts)).append("\n");
    sb.append("    nodes: ").append(toIndentedString(nodes)).append("\n");
    sb.append("    orphanedNodes: ").append(toIndentedString(orphanedNodes)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("hosts", "nodes", "orphanedNodes"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(0);
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to ArgoTree
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!ArgoTree.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in ArgoTree is not found in the empty JSON string", ArgoTree.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!ArgoTree.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `ArgoTree` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // ensure the optional json data is an array if present
      if (jsonObj.get("hosts") != null && !jsonObj.get("hosts").isJsonNull() && !jsonObj.get("hosts").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `hosts` to be an array in the JSON string but got `%s`", jsonObj.get("hosts").toString()));
      }
      if (jsonObj.get("nodes") != null && !jsonObj.get("nodes").isJsonNull()) {
        JsonArray jsonArraynodes = jsonObj.getAsJsonArray("nodes");
        if (jsonArraynodes != null) {
          // ensure the json data is an array
          if (!jsonObj.get("nodes").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `nodes` to be an array in the JSON string but got `%s`", jsonObj.get("nodes").toString()));
          }

          // validate the optional field `nodes` (array)
          for (int i = 0; i < jsonArraynodes.size(); i++) {
            ArgoNode.validateJsonElement(jsonArraynodes.get(i));
          };
        }
      }
      if (jsonObj.get("orphanedNodes") != null && !jsonObj.get("orphanedNodes").isJsonNull()) {
        JsonArray jsonArrayorphanedNodes = jsonObj.getAsJsonArray("orphanedNodes");
        if (jsonArrayorphanedNodes != null) {
          // ensure the json data is an array
          if (!jsonObj.get("orphanedNodes").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `orphanedNodes` to be an array in the JSON string but got `%s`", jsonObj.get("orphanedNodes").toString()));
          }

          // validate the optional field `orphanedNodes` (array)
          for (int i = 0; i < jsonArrayorphanedNodes.size(); i++) {
            ArgoNode.validateJsonElement(jsonArrayorphanedNodes.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!ArgoTree.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'ArgoTree' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<ArgoTree> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(ArgoTree.class));

       return (TypeAdapter<T>) new TypeAdapter<ArgoTree>() {
           @Override
           public void write(JsonWriter out, ArgoTree value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public ArgoTree read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of ArgoTree given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of ArgoTree
   * @throws IOException if the JSON string is invalid with respect to ArgoTree
   */
  public static ArgoTree fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, ArgoTree.class);
  }

  /**
   * Convert an instance of ArgoTree to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

