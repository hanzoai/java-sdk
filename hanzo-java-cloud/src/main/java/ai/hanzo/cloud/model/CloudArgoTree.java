/*
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.hanzo.cloud.model;

import java.util.Objects;
import ai.hanzo.cloud.model.CloudArgoNode;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.hanzo.cloud.JSON;

/**
 * CloudArgoTree
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.14.0")
public class CloudArgoTree {
  public static final String SERIALIZED_NAME_HOSTS = "hosts";
  @SerializedName(SERIALIZED_NAME_HOSTS)
  @javax.annotation.Nullable
  private List<Object> hosts = new ArrayList<>();

  public static final String SERIALIZED_NAME_NODES = "nodes";
  @SerializedName(SERIALIZED_NAME_NODES)
  @javax.annotation.Nullable
  private List<CloudArgoNode> nodes = new ArrayList<>();

  public static final String SERIALIZED_NAME_ORPHANED_NODES = "orphanedNodes";
  @SerializedName(SERIALIZED_NAME_ORPHANED_NODES)
  @javax.annotation.Nullable
  private List<CloudArgoNode> orphanedNodes = new ArrayList<>();

  public CloudArgoTree() {
  }

  public CloudArgoTree hosts(@javax.annotation.Nullable List<Object> hosts) {
    this.hosts = hosts;
    return this;
  }

  public CloudArgoTree addHostsItem(Object hostsItem) {
    if (this.hosts == null) {
      this.hosts = new ArrayList<>();
    }
    this.hosts.add(hostsItem);
    return this;
  }

  /**
   * Get hosts
   * @return hosts
   */
  @javax.annotation.Nullable
  public List<Object> getHosts() {
    return hosts;
  }

  public void setHosts(@javax.annotation.Nullable List<Object> hosts) {
    this.hosts = hosts;
  }


  public CloudArgoTree nodes(@javax.annotation.Nullable List<CloudArgoNode> nodes) {
    this.nodes = nodes;
    return this;
  }

  public CloudArgoTree addNodesItem(CloudArgoNode nodesItem) {
    if (this.nodes == null) {
      this.nodes = new ArrayList<>();
    }
    this.nodes.add(nodesItem);
    return this;
  }

  /**
   * Get nodes
   * @return nodes
   */
  @javax.annotation.Nullable
  public List<CloudArgoNode> getNodes() {
    return nodes;
  }

  public void setNodes(@javax.annotation.Nullable List<CloudArgoNode> nodes) {
    this.nodes = nodes;
  }


  public CloudArgoTree orphanedNodes(@javax.annotation.Nullable List<CloudArgoNode> orphanedNodes) {
    this.orphanedNodes = orphanedNodes;
    return this;
  }

  public CloudArgoTree addOrphanedNodesItem(CloudArgoNode orphanedNodesItem) {
    if (this.orphanedNodes == null) {
      this.orphanedNodes = new ArrayList<>();
    }
    this.orphanedNodes.add(orphanedNodesItem);
    return this;
  }

  /**
   * Get orphanedNodes
   * @return orphanedNodes
   */
  @javax.annotation.Nullable
  public List<CloudArgoNode> getOrphanedNodes() {
    return orphanedNodes;
  }

  public void setOrphanedNodes(@javax.annotation.Nullable List<CloudArgoNode> orphanedNodes) {
    this.orphanedNodes = orphanedNodes;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    CloudArgoTree cloudArgoTree = (CloudArgoTree) o;
    return Objects.equals(this.hosts, cloudArgoTree.hosts) &&
        Objects.equals(this.nodes, cloudArgoTree.nodes) &&
        Objects.equals(this.orphanedNodes, cloudArgoTree.orphanedNodes);
  }

  @Override
  public int hashCode() {
    return Objects.hash(hosts, nodes, orphanedNodes);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class CloudArgoTree {\n");
    sb.append("    hosts: ").append(toIndentedString(hosts)).append("\n");
    sb.append("    nodes: ").append(toIndentedString(nodes)).append("\n");
    sb.append("    orphanedNodes: ").append(toIndentedString(orphanedNodes)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("hosts", "nodes", "orphanedNodes"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(0);
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to CloudArgoTree
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!CloudArgoTree.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in CloudArgoTree is not found in the empty JSON string", CloudArgoTree.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!CloudArgoTree.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `CloudArgoTree` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // ensure the optional json data is an array if present
      if (jsonObj.get("hosts") != null && !jsonObj.get("hosts").isJsonNull() && !jsonObj.get("hosts").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `hosts` to be an array in the JSON string but got `%s`", jsonObj.get("hosts").toString()));
      }
      if (jsonObj.get("nodes") != null && !jsonObj.get("nodes").isJsonNull()) {
        JsonArray jsonArraynodes = jsonObj.getAsJsonArray("nodes");
        if (jsonArraynodes != null) {
          // ensure the json data is an array
          if (!jsonObj.get("nodes").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `nodes` to be an array in the JSON string but got `%s`", jsonObj.get("nodes").toString()));
          }

          // validate the optional field `nodes` (array)
          for (int i = 0; i < jsonArraynodes.size(); i++) {
            CloudArgoNode.validateJsonElement(jsonArraynodes.get(i));
          };
        }
      }
      if (jsonObj.get("orphanedNodes") != null && !jsonObj.get("orphanedNodes").isJsonNull()) {
        JsonArray jsonArrayorphanedNodes = jsonObj.getAsJsonArray("orphanedNodes");
        if (jsonArrayorphanedNodes != null) {
          // ensure the json data is an array
          if (!jsonObj.get("orphanedNodes").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `orphanedNodes` to be an array in the JSON string but got `%s`", jsonObj.get("orphanedNodes").toString()));
          }

          // validate the optional field `orphanedNodes` (array)
          for (int i = 0; i < jsonArrayorphanedNodes.size(); i++) {
            CloudArgoNode.validateJsonElement(jsonArrayorphanedNodes.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!CloudArgoTree.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'CloudArgoTree' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<CloudArgoTree> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(CloudArgoTree.class));

       return (TypeAdapter<T>) new TypeAdapter<CloudArgoTree>() {
           @Override
           public void write(JsonWriter out, CloudArgoTree value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public CloudArgoTree read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of CloudArgoTree given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of CloudArgoTree
   * @throws IOException if the JSON string is invalid with respect to CloudArgoTree
   */
  public static CloudArgoTree fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, CloudArgoTree.class);
  }

  /**
   * Convert an instance of CloudArgoTree to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

