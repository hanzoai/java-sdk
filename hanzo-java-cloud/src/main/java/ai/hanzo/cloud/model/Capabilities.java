/*
 * Hanzo Cloud API
 * The Hanzo Cloud API as a customer calls it: every operation under /v1/ except the operator's admin product, relay routes, legacy spellings and capabilities still reached by flag. Tagged by product: the first path segment after /v1/.
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.hanzo.cloud.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.hanzo.cloud.JSON;

/**
 * Capabilities
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.14.0")
public class Capabilities {
  public static final String SERIALIZED_NAME_ACTIONS = "actions";
  @SerializedName(SERIALIZED_NAME_ACTIONS)
  @javax.annotation.Nullable
  private Boolean actions;

  public static final String SERIALIZED_NAME_DM = "dm";
  @SerializedName(SERIALIZED_NAME_DM)
  @javax.annotation.Nullable
  private Boolean dm;

  public static final String SERIALIZED_NAME_GROUP = "group";
  @SerializedName(SERIALIZED_NAME_GROUP)
  @javax.annotation.Nullable
  private Boolean group;

  public static final String SERIALIZED_NAME_MEDIA = "media";
  @SerializedName(SERIALIZED_NAME_MEDIA)
  @javax.annotation.Nullable
  private Boolean media;

  public static final String SERIALIZED_NAME_THREAD = "thread";
  @SerializedName(SERIALIZED_NAME_THREAD)
  @javax.annotation.Nullable
  private Boolean thread;

  public Capabilities() {
  }

  public Capabilities actions(@javax.annotation.Nullable Boolean actions) {
    this.actions = actions;
    return this;
  }

  /**
   * Actions is whether the transport renders an INTERACTIVE control natively, and it is the flag to read before composing one. The vocabulary is a closed kind-tagged union (envelope.go), exactly four kinds, each carrying only its own field plus an optional label:   command  — a bot command to run (&#x60;command&#x60;), rendered as a button that             invokes it.  url      — an external link (&#x60;url&#x60;), rendered as a link button.  select   — a menu (&#x60;options&#x60;, each a label and the value choosing it             returns), rendered as a picker.  approval — a reference to an approval request (&#x60;approval.id&#x60;), rendered as             approve/deny controls bound to that id.  False on every transport, and nothing refuses a send for it: actions are accepted, validated per kind, and flattened by renderText to one line each after the text — &#x60;[label] command&#x60;, &#x60;[label] url&#x60;, &#x60;[label] opt | opt&#x60;, &#x60;[label] approval requested: &lt;id&gt;&#x60;. So a caller that needs a real control must read this flag and degrade itself; a caller that only needs the choice communicated can send actions and take the text form.
   * @return actions
   */
  @javax.annotation.Nullable
  public Boolean getActions() {
    return actions;
  }

  public void setActions(@javax.annotation.Nullable Boolean actions) {
    this.actions = actions;
  }


  public Capabilities dm(@javax.annotation.Nullable Boolean dm) {
    this.dm = dm;
    return this;
  }

  /**
   * DM is whether the transport carries a DIRECT message at all. True for slack, teams, telegram and whatsapp — whatsapp is nothing else, since the Cloud API addresses a person&#39;s number and there is no room a third party joins. False for discord, honestly: that ingress is guild-scoped slash commands — an interaction without a guild id is refused at the endpoint — so nothing ever arrives classified as a DM, no reply route is ever learned for one, and a send addressed at a Discord DM is refused 409.
   * @return dm
   */
  @javax.annotation.Nullable
  public Boolean getDm() {
    return dm;
  }

  public void setDm(@javax.annotation.Nullable Boolean dm) {
    this.dm = dm;
  }


  public Capabilities group(@javax.annotation.Nullable Boolean group) {
    this.group = group;
    return this;
  }

  /**
   * Group is whether the transport carries multi-person rooms — a Discord guild channel, a Slack channel, a Teams channel or group chat, a Telegram group or supergroup. False on whatsapp alone, which has no such room to carry.
   * @return group
   */
  @javax.annotation.Nullable
  public Boolean getGroup() {
    return group;
  }

  public void setGroup(@javax.annotation.Nullable Boolean group) {
    this.group = group;
  }


  public Capabilities media(@javax.annotation.Nullable Boolean media) {
    this.media = media;
    return this;
  }

  /**
   * Media is whether the transport renders an ATTACHMENT natively. False everywhere, and a send is not refused for it: renderText flattens each attachment to one &#x60;kind: url (mime)&#x60; line after the text rather than dropping it. A transport whose egress passes the raw text straight through would drop the attachment instead, and an attachment-only send would reach the platform with nothing to say — which is why the flag and the flattening are pinned together.
   * @return media
   */
  @javax.annotation.Nullable
  public Boolean getMedia() {
    return media;
  }

  public void setMedia(@javax.annotation.Nullable Boolean media) {
    this.media = media;
  }


  public Capabilities thread(@javax.annotation.Nullable Boolean thread) {
    this.thread = thread;
    return this;
  }

  /**
   * Thread is whether a reply can be threaded UNDER a specific message. True for slack alone: it is the only transport whose ingress reports a thread (thread_ts, published as the envelope&#39;s replyTo) and whose send posts back into it. Discord&#39;s replyTo makes an inline reply rather than a thread, Telegram&#39;s and WhatsApp&#39;s each quote one message, and Teams carries no reply target at all — a replyTo sent to it is ignored.
   * @return thread
   */
  @javax.annotation.Nullable
  public Boolean getThread() {
    return thread;
  }

  public void setThread(@javax.annotation.Nullable Boolean thread) {
    this.thread = thread;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    Capabilities capabilities = (Capabilities) o;
    return Objects.equals(this.actions, capabilities.actions) &&
        Objects.equals(this.dm, capabilities.dm) &&
        Objects.equals(this.group, capabilities.group) &&
        Objects.equals(this.media, capabilities.media) &&
        Objects.equals(this.thread, capabilities.thread);
  }

  @Override
  public int hashCode() {
    return Objects.hash(actions, dm, group, media, thread);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class Capabilities {\n");
    sb.append("    actions: ").append(toIndentedString(actions)).append("\n");
    sb.append("    dm: ").append(toIndentedString(dm)).append("\n");
    sb.append("    group: ").append(toIndentedString(group)).append("\n");
    sb.append("    media: ").append(toIndentedString(media)).append("\n");
    sb.append("    thread: ").append(toIndentedString(thread)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("actions", "dm", "group", "media", "thread"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(0);
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to Capabilities
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!Capabilities.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in Capabilities is not found in the empty JSON string", Capabilities.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!Capabilities.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `Capabilities` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!Capabilities.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'Capabilities' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<Capabilities> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(Capabilities.class));

       return (TypeAdapter<T>) new TypeAdapter<Capabilities>() {
           @Override
           public void write(JsonWriter out, Capabilities value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public Capabilities read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of Capabilities given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of Capabilities
   * @throws IOException if the JSON string is invalid with respect to Capabilities
   */
  public static Capabilities fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, Capabilities.class);
  }

  /**
   * Convert an instance of Capabilities to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

