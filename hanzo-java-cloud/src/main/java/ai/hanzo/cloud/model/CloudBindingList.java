/*
 * Hanzo Cloud — Unified API (V8 · Open Edition)
 * The single unified OpenAPI surface for ALL Hanzo services, aggregated from the per-service specs. Every route is https://api.hanzo.ai/v1/<service>/_*. Grouped by the canonical domains in capabilities.yaml (the ONE registry). cloud/openapi.yaml is hanzoai/cloud's own woven document, copied verbatim by sync.py, and it wins wherever it and a hand-written spec describe the same route. Regenerated by merge.py — edit the per-service <svc>/openapi.yaml + capabilities.yaml, not this file.
 *
 * The version of the OpenAPI document: 8.0.0
 * Contact: support@hanzo.ai
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.hanzo.cloud.model;

import java.util.Objects;
import ai.hanzo.cloud.model.CloudAgentBinding;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.hanzo.cloud.JSON;

/**
 * CloudBindingList
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.14.0")
public class CloudBindingList {
  public static final String SERIALIZED_NAME_AGENT_BINDINGS = "agentBindings";
  @SerializedName(SERIALIZED_NAME_AGENT_BINDINGS)
  @javax.annotation.Nullable
  private List<CloudAgentBinding> agentBindings = new ArrayList<>();

  public CloudBindingList() {
  }

  public CloudBindingList agentBindings(@javax.annotation.Nullable List<CloudAgentBinding> agentBindings) {
    this.agentBindings = agentBindings;
    return this;
  }

  public CloudBindingList addAgentBindingsItem(CloudAgentBinding agentBindingsItem) {
    if (this.agentBindings == null) {
      this.agentBindings = new ArrayList<>();
    }
    this.agentBindings.add(agentBindingsItem);
    return this;
  }

  /**
   * AgentBindings is one row per bound machine, emitted verbatim as vm reports it so vm stays the single source of truth for the binding shape.
   * @return agentBindings
   */
  @javax.annotation.Nullable
  public List<CloudAgentBinding> getAgentBindings() {
    return agentBindings;
  }

  public void setAgentBindings(@javax.annotation.Nullable List<CloudAgentBinding> agentBindings) {
    this.agentBindings = agentBindings;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    CloudBindingList cloudBindingList = (CloudBindingList) o;
    return Objects.equals(this.agentBindings, cloudBindingList.agentBindings);
  }

  @Override
  public int hashCode() {
    return Objects.hash(agentBindings);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class CloudBindingList {\n");
    sb.append("    agentBindings: ").append(toIndentedString(agentBindings)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("agentBindings"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(0);
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to CloudBindingList
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!CloudBindingList.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in CloudBindingList is not found in the empty JSON string", CloudBindingList.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!CloudBindingList.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format("The field `%s` in the JSON string is not defined in the `CloudBindingList` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("agentBindings") != null && !jsonObj.get("agentBindings").isJsonNull()) {
        JsonArray jsonArrayagentBindings = jsonObj.getAsJsonArray("agentBindings");
        if (jsonArrayagentBindings != null) {
          // ensure the json data is an array
          if (!jsonObj.get("agentBindings").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `agentBindings` to be an array in the JSON string but got `%s`", jsonObj.get("agentBindings").toString()));
          }

          // validate the optional field `agentBindings` (array)
          for (int i = 0; i < jsonArrayagentBindings.size(); i++) {
            CloudAgentBinding.validateJsonElement(jsonArrayagentBindings.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!CloudBindingList.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'CloudBindingList' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<CloudBindingList> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(CloudBindingList.class));

       return (TypeAdapter<T>) new TypeAdapter<CloudBindingList>() {
           @Override
           public void write(JsonWriter out, CloudBindingList value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public CloudBindingList read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of CloudBindingList given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of CloudBindingList
   * @throws IOException if the JSON string is invalid with respect to CloudBindingList
   */
  public static CloudBindingList fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, CloudBindingList.class);
  }

  /**
   * Convert an instance of CloudBindingList to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

